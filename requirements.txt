# Fully pinned environment verified with Python 3.12.13 on Windows 11.
#
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#   .venv\Scripts\python.exe -m pip install --no-deps -e .
#
# The pins match pyproject.toml; this file exists so an air-gapped host can be
# provisioned from a wheelhouse without re-resolving anything. `langchain-*` is
# required only by `python -m rag_service.build_cosine`, which unpickles the
# docstore written by the upstream index builder.
#
# Platform-specific pins keep their environment marker. `pywin32` is pulled in by
# `mcp` only on Windows; pinning it unconditionally made `pip install -r
# requirements.txt` fail on Linux and macOS, where no wheel exists.
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
colorama==0.4.6
dataclasses-json==0.6.7
faiss-cpu==1.9.0
fastapi==0.115.8
frozenlist==1.8.0
greenlet==3.5.5
h11==0.16.0
httpcore==1.0.9
httpx==0.27.2
httpx-sse==0.4.3
idna==3.19
iniconfig==2.3.0
jsonpatch==1.33
jsonpointer==3.1.1
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
langchain==0.3.19
langchain-community==0.3.17
langchain-core==0.3.35
langchain-text-splitters==0.3.6
langsmith==0.3.45
marshmallow==3.26.2
mcp==1.12.4
multidict==6.8.0
mypy-extensions==1.1.0
numpy==2.2.3
orjson==3.12.0
packaging==24.2
pluggy==1.6.0
propcache==0.5.2
pydantic==2.9.2
pydantic-core==2.23.4
pydantic-settings==2.15.0
pygments==2.21.0
pytest==9.1.1
python-dotenv==1.2.3
python-multipart==0.0.32
pywin32==312; sys_platform == "win32"
pyyaml==6.0.3
referencing==0.37.0
requests==2.34.2
requests-toolbelt==1.0.0
rpds-py==2026.6.3
sniffio==1.3.1
sqlalchemy==2.0.52
sse-starlette==3.0.3
starlette==0.45.3
tenacity==9.1.4
typing-extensions==4.16.0
typing-inspect==0.9.0
typing-inspection==0.4.4
urllib3==2.7.0
uvicorn==0.34.0
yarl==1.24.5
zstandard==0.23.0

# Installed from this repository, not from PyPI:
#   -e .
